# 🧠 Proyecto de Prueba — Interfaz Demo con Streamlit

¡Gracias por ayudarnos a probar este prototipo! 🙌  
Tu participación es muy valiosa para mejorar esta herramienta.

Este proyecto ofrece una interfaz gráfica simple en Streamlit para interactuar con un modelo o flujo automatizado en tiempo real.

---

## 📌 Requisitos previos

Antes de comenzar, necesitas contar con:

- Sistema operativo **Windows 10 o superior**
- **Conexión a Internet** para instalación inicial

---

## 🚀 Instalación y Ejecución

---

### 🆕 Primera vez instalando el proyecto

Sigue los pasos en orden:

1. **Instalar Python**
   - Tomar el instalador de python que viene en la carpeta
   - Importante: marcar **Add Python to PATH**
   - Clickear **Install python**

2. **Abrir la consola de comandos**
   - Presiona `Windows + R`
   - Escribe: `cmd`
   - Presiona **Enter**

3. **Ir al directorio donde está el proyecto**
   ```bash
   cd Downloads/proyecto

4. **Crear un entorno virtual**
    python -m venv venv_bot

5. **Activar el entorno virtual**
    venv_bot\Scripts\activate

6. **Instalar dependencias**
    pip install -r requirements/dev.txt

7. **Ejecutar la aplicación (Front en Streamlit)**
    streamlit run main_streamlit.py

8. **Cerrar la aplicación**
    Si se desea cerrar la herramienta oprima **Ctrl + C**

### 🆕 Primera vez instalando el proyecto

1. **Abrir la consola de comandos**
   - Presiona `Windows + R`
   - Escribe: `cmd`
   - Presiona **Enter**

2. **Ir al directorio donde está el proyecto**
   ```bash
   cd Downloads/proyecto

3. **Activar el entorno virtual**
    venv_bot\Scripts\activate

4. **Ejecutar la aplicación (Front en Streamlit)**
    streamlit run main_streamlit.py

5. **Cerrar la aplicación**
    Si se desea cerrar la herramienta oprima **Ctrl + C**